import { prisma } from "@/lib/prisma";import { AdminAction } from "@/components/admin-action";
export default async function ArchivePage(){const rows=await prisma.archiveRecord.findMany({where:{restoredAt:null},include:{survey:{include:{_count:{select:{responses:true}}}}},orderBy:{archivedAt:"desc"}});return <><div className="page-title"><div><h1>Архив</h1><p className="muted">Логический архив сохраняет опросы и ответы; это не резервная копия</p></div></div><div className="card table-card"><table className="data-table"><thead><tr><th>Опрос</th><th>Архивирован</th><th>Ответы</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.survey.title}</b></td><td>{r.archivedAt.toLocaleString("ru-RU")}</td><td>{r.survey._count.responses}</td><td><AdminAction url={`/api/admin/surveys/${r.surveyId}/restore`} label="Восстановить в CLOSED"/></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Архив пуст</div>}</div></>}
