import { prisma } from "@/lib/prisma";
export default async function Audit(){const rows=await prisma.auditLog.findMany({include:{user:true},orderBy:{createdAt:"desc"},take:100});return <><div className="page-title"><div><h1>Журнал действий</h1><p className="muted">Последние 100 административных событий</p></div></div><div className="card table-card"><table className="data-table"><thead><tr><th>Время</th><th>Действие</th><th>Объект</th><th>Администратор</th><th>Детали</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.createdAt.toLocaleString("ru-RU")}</td><td><b>{r.action}</b></td><td>{r.entityType}<div className="hint">{r.entityId}</div></td><td>{r.user?.email??"Система"}</td><td className="hint">{r.detailsJson?.slice(0,100)??"—"}</td></tr>)}</tbody></table></div></>}
