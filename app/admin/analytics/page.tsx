import { analyticsData, filterOptions, type Filters } from "@/lib/data";
import FilterBar from "@/components/filter-bar";
import AnalyticsCharts from "@/components/analytics-charts";
import MetricCards from "@/components/metric-cards";

export default async function Analytics({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const filters=await searchParams as Filters;const [data,options]=await Promise.all([analyticsData(filters),filterOptions()]);const distribution=[1,2,3,4,5].map(rating=>({rating:String(rating),count:data.responses.filter(r=>r.answers.some(a=>a.questionCodeSnapshot==="OVERALL_SATISFACTION"&&a.numericValue===rating)).length}));return <><div className="page-title"><div><h1>Аналитика</h1><p className="muted">Единые фильтры применяются к карточкам, графикам и экспорту</p></div><div className="toolbar"><a className="button secondary" href={`/api/exports/csv?${new URLSearchParams(filters as Record<string,string>)}`}>CSV</a><a className="button" href={`/api/exports/xlsx?${new URLSearchParams(filters as Record<string,string>)}`}>XLSX</a></div></div><FilterBar options={options} values={filters as Record<string,string|undefined>}/><MetricCards m={data.metrics}/>{data.metrics.responseCount<5&&<div className="notice">Выборка содержит менее 5 ответов. Интерпретируйте показатели с осторожностью.</div>}<AnalyticsCharts trend={data.trend} distribution={distribution}/></>}
