import { prisma } from "@/lib/prisma";import DirectoryToggle from "@/components/directory-toggle";
export default async function Directories(){const groups=await Promise.all([prisma.department.findMany({orderBy:{name:"asc"}}),prisma.project.findMany({orderBy:{name:"asc"}}),prisma.respondentRole.findMany({orderBy:{name:"asc"}}),prisma.collaborationFormat.findMany({orderBy:{name:"asc"}}),prisma.processOfficeDirection.findMany({orderBy:{name:"asc"}})]);const meta=[["departments","Подразделения"],["projects","Проекты"],["roles","Роли"],["formats","Форматы"],["directions","Направления"]] as const;return <><div className="page-title"><div><h1>Справочники</h1><p className="muted">Использованные значения не удаляются физически</p></div></div><div className="dashboard-grid">{groups.map((items,i)=><section className="card" style={{padding:20}} key={meta[i][0]}><h3>{meta[i][1]}</h3><table className="data-table"><tbody>{items.map(x=><tr key={x.id}><td>{x.name}<div className="hint">{x.active?"Активно":"Неактивно"}</div></td><td style={{textAlign:"right"}}><DirectoryToggle type={meta[i][0]} id={x.id} active={x.active}/></td></tr>)}</tbody></table></section>)}</div></>}
