type Item={id:string;name?:string;title?:string};
export default function FilterBar({options,values={},action="/admin/analytics"}:{options:{surveys:Item[];departments:Item[];projects:Item[];formats:Item[]};values?:Record<string,string|undefined>;action?:string}){return <form className="card filterbar" action={action}><Select name="surveyId" label="Все опросы" items={options.surveys} value={values.surveyId}/><Select name="departmentId" label="Все подразделения" items={options.departments} value={values.departmentId}/><Select name="projectId" label="Все проекты" items={options.projects} value={values.projectId}/><Select name="formatId" label="Все форматы" items={options.formats} value={values.formatId}/><select name="anonymityMode" defaultValue={values.anonymityMode??""} aria-label="Анонимность"><option value="">Любая анонимность</option><option value="ANONYMOUS">Анонимные</option><option value="IDENTIFIED">Идентифицированные</option></select><button className="button">Применить</button></form>}
function Select({name,label,items,value}:{name:string;label:string;items:Item[];value?:string}){return <select name={name} defaultValue={value??""} aria-label={label}><option value="">{label}</option>{items.map(i=><option key={i.id} value={i.id}>{i.name??i.title}</option>)}</select>}
