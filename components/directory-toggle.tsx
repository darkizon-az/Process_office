"use client";import { useRouter } from "next/navigation";export default function DirectoryToggle({type,id,active}:{type:string;id:string;active:boolean}){const r=useRouter();return <button className={`button ${active?"danger":"secondary"}`} onClick={async()=>{await fetch(`/api/admin/directories/${type}/${id}`,{method:"POST"});r.refresh()}}>{active?"Деактивировать":"Активировать"}</button>}
